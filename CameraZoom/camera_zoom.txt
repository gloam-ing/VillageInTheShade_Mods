# Camera Zoom Range Mod 配置
# 修改后保存，重新启动游戏生效。

Enabled = 1
Patch = 1
PatchHeap = 0
# 1 = 扫描并记录引用倍率表的代码地址（调试用，会刷日志）；0 = 关闭
RefScan = 0

# 加载 lz4.dll 后延迟多少毫秒开始补丁（游戏启动时加载 lz4.dll）
DelayMs = 10000
# 每轮补丁间隔（毫秒）；0 = 只执行一次
RepeatMs = 0

# 三档倍率目标值（近/中/远）
# 注意：这是"放大倍率"，值越小镜头越远、看到的范围越大。
# 原版 1.2 / 1.35 / 1.5
# 0.4 太远会导致远景渲染断层；出现断层就把数值调大
Zoom1 = 0.6
Zoom2 = 0.9
Zoom3 = 1.2
